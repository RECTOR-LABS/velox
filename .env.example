# Velox — copy to .env (gitignored). Never commit real values.
GOOGLE_GENAI_USE_VERTEXAI=TRUE
GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=us-central1
# Set MDB_MCP_CONNECTION_STRING as a Cloud Run env var (secret) — never a CLI arg
MDB_MCP_CONNECTION_STRING=
